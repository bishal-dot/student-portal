@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model StudentPortal.Models.Entities.Student

<h1>Edit Student</h1>

@if (Model is null)
{
    <p>No student was found with this Id </p>
}
else{
<form method="post">
    <div class="mt-3">
        <label>Id</label>
        <input type="text" class="form-control" placeholder="Enter student name" asp-for="Id" readonly />
    </div>

    <div class="mt-3">
        <label>Name</label>
        <input type="text" class="form-control" placeholder="Enter student name" asp-for="Name" />
    </div>
    <div class="mt-3">
        <label>Email</label>
        <input type="email" class="form-control" placeholder="Enter student email" asp-for="Email" />
    </div>
    <div class="mt-3">
        <label class="mt-2">Phone</label>
        <input type="text" class="form-control" placeholder="Enter student phone" asp-for="Phone" />
    </div>
    <button type="submit" class="btn btn-primary mt-3">Update</button>
    <button class="btn btn-secondary mt-3 ms-2">Cancel</button>
</form>
}

